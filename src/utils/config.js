// src/utils/config.js
const config = {
  BASE_URL: `${window.location.origin}`, // API dựa trên host hiện tại,
  //BASE_URL: process.env.REACT_APP_BASE_URL || "http://localhost:3000",
  API_URL: 'http://localhost:5000'
}
export default config
